import React from 'react';
import PropTypes from 'prop-types';


const Modal = ({ state }) => (
  <div>
    Modal Component {state}
  </div>
);

Modal.propTypes = {};

Modal.defaultProps = {};

export default Modal;
